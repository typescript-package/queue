import { ArrayState } from "@typescript-package/state";
/**
 * @description Array state elements in data structures such as Stack and Queue.
 * @export
 * @class Elements
 * @template Type 
 * @extends {ArrayState<Type>}
 */
export class Elements<Type> extends ArrayState<Type>{};
